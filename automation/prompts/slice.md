# Slice Brief

Current queued slice:

- `slice_id`: `__SLICE_ID__`
- `title`: __SLICE_TITLE__
- `domain`: `__SLICE_DOMAIN__`
- `priority`: `__SLICE_PRIORITY__`
- `allowed_paths`:
__ALLOWED_PATHS__
- `required_validations`:
__REQUIRED_VALIDATIONS__
- `validation_ownership`:
__VALIDATION_OWNERSHIP__
- `max_files_changed`: `__DIFF_BUDGET__`
- `depends_on`:
__DEPENDENCIES__
- `notes`: __SLICE_NOTES__

Policy reminder:

> __POLICY_SENTENCE__

Adjacent queued slices you may recommend by ID if they are the honest follow-up:

__ADJACENT_SLICE_CANDIDATES__

Previous handoff summary:

__PREVIOUS_HANDOFF_SUMMARY__

Queue metadata for this run:

```json
__QUEUE_METADATA_JSON__
```

Acceptance checks for this slice:

__ACCEPTANCE_CHECKS__

Maintained docs included in this context package:

__CONTEXT_DOCUMENT_INDEX__

Compact context artifact:

```json
__CONTEXT_JSON__
```

Write exactly one JSON handoff artifact to:

`__HANDOFF_PATH__`

Use this starter shape and replace the placeholders with honest run results:

```json
__HANDOFF_TEMPLATE_JSON__
```
